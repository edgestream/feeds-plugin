#!/usr/bin/env node
var FeedError=class extends Error{constructor(code,message,options){super(message,options);this.code=code;this.name="FeedError"}code};var FeedService=class{platforms;providers;constructor(platforms,providers){if(new Set(platforms.map(p=>p.id)).size!==platforms.length||new Set(providers.map(p=>p.platform)).size!==providers.length||providers.some(p=>!platforms.some(platform=>platform.id===p.platform))){throw new FeedError("CONFIGURATION","Expected unique platforms and one selected provider per platform.")}this.platforms=[...platforms];this.providers=new Map(providers.map(provider=>[provider.platform,provider]))}async show(input,options={},context={}){if(context.signal?.aborted)throw new FeedError("CANCELLED","Request cancelled.",{cause:context.signal.reason});let url;try{url=new URL(input)}catch{throw new FeedError("INVALID_INPUT","Expected a complete public post or profile URL.")}const platform=this.platforms.find(platform2=>platform2.supports(url));if(!platform)throw new FeedError("INVALID_INPUT","The URL does not belong to a supported platform.");const provider=this.providers.get(platform.id);if(!provider)throw new FeedError("CONFIGURATION",`No provider configured for platform ${platform.id}.`);return provider.get(url,options,context)}};var BlueskyPlatform=class{id="bluesky";supports(url){if(url.hostname!=="bsky.app")return false;const match=/^\/profile\/([^/]+)(?:\/post\/([a-zA-Z0-9._~:-]{1,512}))?\/?$/u.exec(url.pathname);const actor=match?.[1]??"";const handle=actor.length<=253&&/^(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?$/u.test(actor);const did=/^did:plc:[a-z2-7]{24}$/u.test(actor)||actor.length<=261&&/^did:web:(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?$/u.test(actor);if(url.hostname!=="bsky.app"||!["http:","https:"].includes(url.protocol)||url.username||url.password||url.port||!match||!handle&&!did||match[2]==="."||match[2]===".."){throw new FeedError("INVALID_INPUT","Expected a public Bluesky post or profile URL.")}return true}};var hosts=new Set(["x.com","www.x.com","twitter.com","www.twitter.com","mobile.twitter.com"]);var XPlatform=class{id="x";supports(url){if(!hosts.has(url.hostname))return false;const match=/^\/(?:[a-zA-Z0-9_]{1,15}|i\/web)\/status\/([0-9]+)(?:\/(?:photo|video)\/[1-9][0-9]*)?\/?$/u.exec(url.pathname);const author=/^\/([a-zA-Z0-9_]{1,15})\/?$/u.exec(url.pathname);const reserved=new Set(["home","explore","search","notifications","messages","settings","i","intent","compose","login","logout","signup","tos","privacy"]);if(!["http:","https:"].includes(url.protocol)||url.username||url.password||url.port||!match&&(!author||reserved.has(author[1].toLowerCase()))){throw new FeedError("INVALID_INPUT","Expected a public X post or profile URL.")}return true}};var version="0.2.0";async function request(fetch,platform,path,context){const label=platform==="x"?"fxTwitter":"FxBluesky";if(context.signal?.aborted)throw new FeedError("CANCELLED","Request cancelled.",{cause:context.signal.reason});try{const response=await fetch(`${platform==="x"?"https://api.fxtwitter.com":"https://api.fxbsky.app"}/2/${path}`,{headers:{Accept:"application/json","User-Agent":`feeds-plugin/${version} (read-only)`},redirect:"error",...context.signal?{signal:context.signal}:{}});if(response.status===404)throw new FeedError("NOT_FOUND",`${label} returned HTTP 404.`);if(response.status===429)throw new FeedError("RATE_LIMITED",`${label} returned HTTP 429.`);if(!response.ok)throw new FeedError("UPSTREAM",`${label} returned HTTP ${response.status}.`);return await response.json()}catch(cause){if(context.signal?.aborted)throw new FeedError("CANCELLED","Request cancelled.",{cause});if(cause instanceof FeedError)throw cause;if(cause instanceof SyntaxError)throw new FeedError("INVALID_RESPONSE",cause.message,{cause});throw new FeedError("UPSTREAM",cause instanceof Error?cause.message:`${label} request failed.`,{cause})}}var FxBlueskyProvider=class{id="fxtwitter";platform="bluesky";fetch;constructor(options={}){this.fetch=options.fetch??globalThis.fetch}async get(url,options={},context={}){const match=/^\/profile\/([^/]+)(?:\/post\/([a-zA-Z0-9._~:-]{1,512}))?\/?$/u.exec(url.pathname);const actor=match?.[1]??"";const handle=actor.length<=253&&/^(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?$/u.test(actor);const did=/^did:plc:[a-z2-7]{24}$/u.test(actor)||actor.length<=261&&/^did:web:(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?$/u.test(actor);if(url.hostname!=="bsky.app"||!["http:","https:"].includes(url.protocol)||url.username||url.password||url.port||!match||!handle&&!did||match[2]==="."||match[2]===".."){throw new FeedError("INVALID_INPUT","Expected a public Bluesky post or profile URL.")}const post=match[2];if(!post&&options.context)throw new FeedError("INVALID_INPUT","Context requires a post URL.");if(options.cursor!==void 0&&(typeof options.cursor!=="string"||!options.cursor||post&&!options.answers)){throw new FeedError("INVALID_INPUT","Cursor must be a nonempty string and requires a profile URL or a post URL with answers enabled.")}const encodedActor=encodeURIComponent(handle?actor.toLowerCase():actor);const path=post?`${options.answers?"conversation":options.context?"thread":"status"}/${encodedActor}/${encodeURIComponent(post)}`:`profile/${encodedActor}/statuses`;const parameters=new URLSearchParams;if(!post&&options.answers)parameters.set("with_replies","1");if(options.cursor!==void 0)parameters.set("cursor",options.cursor);return request(this.fetch,this.platform,path+(parameters.size?`?${parameters}`:""),context)}};var FxTwitterProvider=class{id="fxtwitter";platform="x";fetch;constructor(options={}){this.fetch=options.fetch??globalThis.fetch}async get(url,options={},context={}){const post=/^\/(?:[a-zA-Z0-9_]{1,15}|i\/web)\/status\/([0-9]+)(?:\/(?:photo|video)\/[1-9][0-9]*)?\/?$/u.exec(url.pathname);const author=/^\/([a-zA-Z0-9_]{1,15})\/?$/u.exec(url.pathname);const hosts2=["x.com","www.x.com","twitter.com","www.twitter.com","mobile.twitter.com"];const reserved=["home","explore","search","notifications","messages","settings","i","intent","compose","login","logout","signup","tos","privacy"];if(!hosts2.includes(url.hostname)||!["http:","https:"].includes(url.protocol)||url.username||url.password||url.port||!post&&(!author||reserved.includes(author[1].toLowerCase()))){throw new FeedError("INVALID_INPUT","Expected a public X post or profile URL.")}if(!post&&options.context)throw new FeedError("INVALID_INPUT","Context requires a post URL.");if(options.cursor!==void 0&&(typeof options.cursor!=="string"||!options.cursor||post&&!options.answers)){throw new FeedError("INVALID_INPUT","Cursor must be a nonempty string and requires a profile URL or a post URL with answers enabled.")}const path=post?`${options.answers?"conversation":options.context?"thread":"status"}/${post[1]}`:`profile/${author[1].toLowerCase()}/statuses`;const parameters=new URLSearchParams;if(!post&&options.answers)parameters.set("with_replies","1");if(options.cursor!==void 0)parameters.set("cursor",options.cursor);const query=parameters.size?`?${parameters}`:"";return request(this.fetch,this.platform,`${path}${query}`,context)}};function readConfiguration(env=process.env){return{xProvider:env.FEEDS_X_PROVIDER??"fxtwitter",blueskyProvider:env.FEEDS_BLUESKY_PROVIDER??"fxtwitter"}}function createFeed(configuration=readConfiguration(),options={}){const providers=new Map([["fxtwitter",()=>new FxTwitterProvider(options)]]);const createProvider=providers.get(configuration.xProvider);if(!createProvider)throw new FeedError("CONFIGURATION",`Unknown X provider: ${configuration.xProvider}.`);const blueskyProvider=configuration.blueskyProvider??"fxtwitter";if(blueskyProvider!=="fxtwitter")throw new FeedError("CONFIGURATION",`Unknown Bluesky provider: ${blueskyProvider}.`);return new FeedService([new XPlatform,new BlueskyPlatform],[createProvider(),new FxBlueskyProvider(options)])}var usage="Usage: feeds show [--context] [--answers] [--cursor <cursor>] <post-or-profile-url>\n";async function runCli(args,createService,output,context={}){try{if(args.length===1&&(args[0]==="--help"||args[0]==="-h")){output.stdout(usage);return 0}if(args[0]!=="show")throw new FeedError("INVALID_INPUT",usage.trim());let input;let cursor;let contextOption=false,answers=false;const seen=new Set;for(let index=1;index<args.length;index++){const argument=args[index];if(argument.startsWith("-")){if(seen.has(argument))throw new FeedError("INVALID_INPUT",`Duplicate option: ${argument}`);seen.add(argument);if(argument==="--context")contextOption=true;else if(argument==="--answers")answers=true;else if(argument==="--cursor"){const value=args[++index];if(!value||value.startsWith("--"))throw new FeedError("INVALID_INPUT","--cursor requires a nonempty value.");cursor=value}else throw new FeedError("INVALID_INPUT",`Unknown option: ${argument}`)}else{if(input||!argument)throw new FeedError("INVALID_INPUT",usage.trim());input=argument}}if(!input)throw new FeedError("INVALID_INPUT",usage.trim());const result=await createService().show(input,{context:contextOption,answers,...cursor!==void 0?{cursor}:{}},context);output.stdout(`${JSON.stringify(result,null,2)}
`);return 0}catch(error){const failure=error instanceof FeedError?error:new FeedError("UPSTREAM","Unexpected request failure.");output.stderr(`${failure.code}: ${failure.message}
`);return failure.code==="INVALID_INPUT"?2:failure.code==="CANCELLED"?130:1}}var controller=new AbortController;var cancel=()=>controller.abort();process.once("SIGINT",cancel);try{process.exitCode=await runCli(process.argv.slice(2),()=>createFeed(),{stdout:text=>{process.stdout.write(text)},stderr:text=>{process.stderr.write(text)}},{signal:controller.signal})}finally{process.removeListener("SIGINT",cancel)}
