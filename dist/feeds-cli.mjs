#!/usr/bin/env node
var FeedError=class extends Error{constructor(code,message,options){super(message,options);this.code=code;this.name="FeedError"}code};var FeedService=class{platforms;providers;constructor(platforms,providers){if(new Set(platforms.map(p=>p.id)).size!==platforms.length||new Set(providers.map(p=>p.platform)).size!==providers.length||providers.some(p=>!platforms.some(platform=>platform.id===p.platform))){throw new FeedError("CONFIGURATION","Expected unique platforms and one selected provider per platform.")}this.platforms=[...platforms];this.providers=new Map(providers.map(provider=>[provider.platform,provider]))}async show(input,context={}){if(context.signal?.aborted)throw new FeedError("CANCELLED","Request cancelled.");let url;try{url=new URL(input)}catch{throw new FeedError("INVALID_INPUT","Expected a complete public post URL.")}for(const platform of this.platforms){const ref=platform.resolve(url);if(!ref)continue;const provider=this.providers.get(ref.platform);if(!provider)throw new FeedError("CONFIGURATION",`No provider configured for platform ${ref.platform}.`);return provider.get(ref,context)}throw new FeedError("INVALID_INPUT","The post URL does not belong to a supported platform.")}};var hosts=new Set(["x.com","www.x.com","twitter.com","www.twitter.com","mobile.twitter.com"]);var XPlatform=class{id="x";resolve(url){if(!hosts.has(url.hostname))return void 0;const match=/^\/(?:[a-zA-Z0-9_]{1,15}|i\/web)\/status\/([0-9]+)(?:\/(?:photo|video)\/[1-9][0-9]*)?\/?$/u.exec(url.pathname);if(!["http:","https:"].includes(url.protocol)||url.username||url.password||url.port||!match){throw new FeedError("INVALID_INPUT","Expected a public X post URL: https://x.com/handle/status/id")}return{platform:this.id,id:match[1]}}};var FxTwitterProvider=class{id="fxtwitter";platform="x";fetch;timeoutMs;maxResponseBytes;constructor(options={}){this.fetch=options.fetch??globalThis.fetch;this.timeoutMs=options.timeoutMs??2e4;this.maxResponseBytes=options.maxResponseBytes??5*1024*1024;if(![this.timeoutMs,this.maxResponseBytes].every(value=>Number.isSafeInteger(value)&&value>0)||this.timeoutMs>2147483647){throw new FeedError("CONFIGURATION","Request limits must be positive safe integers; timeout must fit a Node timer.")}}async get(ref,context={}){if(ref.platform!==this.platform||!/^[0-9]+$/u.test(ref.id)){throw new FeedError("INVALID_INPUT","fxTwitter requires an X post reference with a numeric ID.")}if(context.signal?.aborted)throw new FeedError("CANCELLED","Request cancelled.");const controller2=new AbortController;const cancel2=()=>controller2.abort();context.signal?.addEventListener("abort",cancel2,{once:true});let timedOut=false;const timer=setTimeout(()=>{timedOut=true;controller2.abort()},this.timeoutMs);let response;try{response=await this.fetch(`https://api.fxtwitter.com/2/status/${ref.id}`,{headers:{Accept:"application/json","User-Agent":"feeds-plugin/0.1.0 (read-only)"},redirect:"error",signal:controller2.signal});if(response.status===404)throw new FeedError("NOT_FOUND","fxTwitter could not find the post.");if(response.status===429)throw new FeedError("RATE_LIMITED","fxTwitter rate limit exceeded.");if(!response.ok)throw new FeedError("UPSTREAM",`fxTwitter returned HTTP ${response.status}.`);if(Number(response.headers.get("content-length"))>this.maxResponseBytes){throw new FeedError("INVALID_RESPONSE","fxTwitter response exceeds the size limit.")}if(!response.body)throw new FeedError("INVALID_RESPONSE","fxTwitter returned an empty response.");const reader=response.body.getReader();const chunks=[];let size=0;try{while(true){const{done,value}=await reader.read();if(done)break;size+=value.byteLength;if(size>this.maxResponseBytes)throw new FeedError("INVALID_RESPONSE","fxTwitter response exceeds the size limit.");chunks.push(value)}}finally{await reader.cancel().catch(()=>{});reader.releaseLock()}let payload;try{payload=JSON.parse(new TextDecoder("utf-8",{fatal:true}).decode(Buffer.concat(chunks)))}catch(cause){throw new FeedError("INVALID_RESPONSE","fxTwitter returned invalid JSON.",{cause})}if(payload===null||typeof payload!=="object"||Array.isArray(payload)){throw new FeedError("INVALID_RESPONSE","fxTwitter returned a non-object JSON response.")}return payload}catch(cause){if(context.signal?.aborted)throw new FeedError("CANCELLED","Request cancelled.",{cause});if(timedOut)throw new FeedError("TIMEOUT","fxTwitter request timed out.",{cause});if(cause instanceof FeedError)throw cause;throw new FeedError("UPSTREAM","fxTwitter request failed.",{cause})}finally{clearTimeout(timer);context.signal?.removeEventListener("abort",cancel2);await response?.body?.cancel().catch(()=>{})}}};function readConfiguration(env=process.env){return{xProvider:env.FEEDS_X_PROVIDER??"fxtwitter"}}function createFeed(configuration=readConfiguration(),options={}){const providers=new Map([["fxtwitter",()=>new FxTwitterProvider(options)]]);const createProvider=providers.get(configuration.xProvider);if(!createProvider)throw new FeedError("CONFIGURATION",`Unknown X provider: ${configuration.xProvider}.`);return new FeedService([new XPlatform],[createProvider()])}var usage="Usage: feeds show <post-url>\n";async function runCli(args,createService,output,context={}){try{if(args.length===1&&(args[0]==="--help"||args[0]==="-h")){output.stdout(usage);return 0}if(args.length!==2||args[0]!=="show"||!args[1]||args[1].startsWith("-")){throw new FeedError("INVALID_INPUT",usage.trim())}const result=await createService().show(args[1],context);output.stdout(`${JSON.stringify(result,null,2)}
`);return 0}catch(error){const failure=error instanceof FeedError?error:new FeedError("UPSTREAM","Unexpected request failure.");output.stderr(`${failure.code}: ${failure.message}
`);return failure.code==="INVALID_INPUT"?2:failure.code==="CANCELLED"?130:1}}var controller=new AbortController;var cancel=()=>controller.abort();process.once("SIGINT",cancel);try{process.exitCode=await runCli(process.argv.slice(2),()=>createFeed(),{stdout:text=>{process.stdout.write(text)},stderr:text=>{process.stderr.write(text)}},{signal:controller.signal})}finally{process.removeListener("SIGINT",cancel)}
